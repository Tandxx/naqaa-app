<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>إعادة تعيين كلمة المرور - نقاء</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Noto+Sans+Arabic:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { 
      font-family: 'Noto Sans Arabic', 'Space Grotesk', sans-serif; 
    }
    
    .auth-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-lg);
    }
    
    .auth-card {
      width: 100%;
      max-width: 450px;
      background: var(--gradient-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-xl);
      padding: var(--space-xl);
      backdrop-filter: blur(20px);
      box-shadow: var(--shadow-xl);
    }
    
    .auth-header {
      text-align: center;
      margin-bottom: var(--space-xl);
    }
    
    .auth-logo {
      font-size: 3.5rem;
      margin-bottom: var(--space-md);
      filter: drop-shadow(0 0 20px rgba(36, 132, 242, 0.3));
    }
    
    .auth-title {
      background: var(--gradient-primary);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: var(--space-sm);
      font-family: 'Space Grotesk', sans-serif;
    }
    
    .auth-subtitle {
      color: var(--text-tertiary);
      font-size: 1rem;
      margin: 0;
      line-height: 1.5;
    }
    
    .auth-form {
      display: flex;
      flex-direction: column;
      gap: var(--space-lg);
    }
    
    .form-group {
      display: flex;
      flex-direction: column;
    }
    
    .form-label {
      display: flex;
      align-items: center;
      gap: var(--space-sm);
      margin-bottom: var(--space-sm);
      font-weight: 600;
      color: var(--text-secondary);
    }
    
    .form-control {
      background: var(--bg-input);
      border: 1px solid var(--border-primary);
      color: var(--text-primary);
      transition: all 0.3s ease;
      min-height: 45px;
      padding: var(--space-sm) var(--space-md);
      border-radius: var(--radius-md);
    }
    
    .form-control:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px var(--primary-subtle);
      outline: none;
    }
    
    .form-hint {
      color: var(--text-muted);
      font-size: 0.85rem;
      margin-top: var(--space-xs);
    }
    
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: var(--space-sm);
      min-height: 48px;
      font-weight: 600;
      border-radius: var(--radius-md);
      transition: all 0.3s ease;
      text-decoration: none;
      border: none;
      cursor: pointer;
    }
    
    .btn:hover {
      transform: translateY(-2px);
      text-decoration: none;
    }
    
    .btn-primary {
      background: var(--gradient-primary);
      color: white;
      box-shadow: var(--shadow-glow);
    }
    
    .btn-primary:hover {
      box-shadow: var(--shadow-glow-strong);
      color: white;
    }
    
    .btn-secondary {
      background: var(--bg-card);
      color: var(--text-secondary);
      border: 1px solid var(--border-primary);
    }
    
    .btn-secondary:hover {
      background: var(--bg-card-hover);
      color: var(--text-primary);
      border-color: var(--border-accent);
    }
    
    .auth-footer {
      text-align: center;
      margin-top: var(--space-xl);
      padding-top: var(--space-lg);
      border-top: 1px solid var(--border-primary);
    }
    
    .auth-footer p {
      color: var(--text-tertiary);
      margin-bottom: var(--space-md);
      font-size: 0.9rem;
    }
    
    .alert {
      padding: var(--space-md);
      border-radius: var(--radius-md);
      margin-bottom: var(--space-lg);
      border: 1px solid;
      display: flex;
      align-items: center;
      gap: var(--space-sm);
    }
    
    .alert-danger {
      background: rgba(255, 71, 87, 0.1);
      border-color: var(--border-danger);
      color: var(--danger);
    }
    
    .security-info {
      background: var(--bg-glass);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-md);
      padding: var(--space-md);
      margin: var(--space-md) 0;
    }
    
    .security-header {
      display: flex;
      align-items: center;
      gap: var(--space-sm);
      margin-bottom: var(--space-sm);
    }
    
    /* تحسين validation */
    .form-control.invalid {
      border-color: var(--danger);
      box-shadow: 0 0 0 3px rgba(255, 71, 87, 0.1);
    }
    
    .form-control.valid {
      border-color: var(--success);
      box-shadow: 0 0 0 3px rgba(0, 255, 136, 0.1);
    }
    
    .password-strength {
      margin-top: var(--space-sm);
      font-size: 0.85rem;
    }
    
    .strength-bar {
      height: 4px;
      border-radius: 2px;
      margin: var(--space-xs) 0;
      transition: all 0.3s ease;
    }
    
    .strength-weak { background: var(--danger); width: 33%; }
    .strength-medium { background: var(--warning); width: 66%; }
    .strength-strong { background: var(--success); width: 100%; }
    
    @media (max-width: 768px) {
      .auth-container {
        padding: var(--space-md);
      }
      
      .auth-card {
        max-width: 100%;
        padding: var(--space-lg);
      }
    }
  </style>
</head>
<body>
  <div class="page-wrapper auth-container">
    <div class="auth-card">
      <div class="auth-header">
        <img src="../image/1.png" alt="نقاء" class="auth-logo-img">
        <h1 class="auth-title">كلمة مرور جديدة</h1>
        <p class="auth-subtitle">
          أدخل كلمة المرور الجديدة لحسابك
        </p>
      </div>

      <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger">
          <span>⚠️</span>
          <div>
            <% errors.forEach(error => { %>
              <p style="margin: 0;"><%= error.msg %></p>
            <% }) %>
          </div>
        </div>
      <% } %>

      <form action="/reset-password/<%= token %>" method="POST" class="auth-form" id="resetForm">
        <div class="form-group">
          <label for="password" class="form-label">
            <span>🔒</span>
            <span>كلمة المرور الجديدة</span>
          </label>
          <input 
            type="password" 
            id="password" 
            name="password" 
            class="form-control" 
            placeholder="كلمة مرور قوية"
            required
            minlength="6"
            autocomplete="new-password"
          >
          <small class="form-hint">6 أحرف على الأقل</small>
          <div class="password-strength" id="passwordStrength" style="display: none;">
            <div class="strength-bar" id="strengthBar"></div>
            <span id="strengthText"></span>
          </div>
        </div>

        <div class="form-group">
          <label for="confirmPassword" class="form-label">
            <span>🔐</span>
            <span>تأكيد كلمة المرور</span>
          </label>
          <input 
            type="password" 
            id="confirmPassword" 
            name="confirmPassword" 
            class="form-control" 
            placeholder="أعد كتابة كلمة المرور"
            required
            autocomplete="new-password"
          >
        </div>

        <div class="security-info">
          <div class="security-header">
            <span style="font-size: 1.2rem;">🛡️</span>
            <strong style="color: var(--text-secondary); font-size: 0.9rem;">نصائح للأمان</strong>
          </div>
          <ul style="list-style: none; color: var(--text-tertiary); font-size: 0.85rem; line-height: 1.5; margin: 0; padding: 0;">
            <li style="margin-bottom: var(--space-xs);">✅ استخدم مزيج من الأحرف والأرقام والرموز</li>
            <li style="margin-bottom: var(--space-xs);">✅ تجنب استخدام معلومات شخصية</li>
            <li style="margin-bottom: var(--space-xs);">✅ اجعل كلمة المرور فريدة لهذا الحساب</li>
            <li>✅ احفظ كلمة المرور في مكان آمن</li>
          </ul>
        </div>

        <button type="submit" class="btn btn-primary" style="width: 100%; padding: var(--space-md); font-size: 1.1rem;">
          <span>💾</span>
          <span>حفظ كلمة المرور الجديدة</span>
        </button>
      </form>

      <div class="auth-footer">
        <p>تذكرت كلمة المرور؟</p>
        <a href="/login" class="btn btn-secondary">
          <span>🔑</span>
          <span>تسجيل دخول</span>
        </a>
      </div>
    </div>
  </div>

  <script>
    function checkPasswordStrength(password) {
      let strength = 0;
      let text = '';
      let className = '';
      
      // طول كلمة المرور
      if (password.length >= 6) strength++;
      if (password.length >= 8) strength++;
      
      // أحرف كبيرة وصغيرة
      if (/[a-z]/.test(password) && /[A-Z]/.test(password)) strength++;
      
      // أرقام
      if (/\d/.test(password)) strength++;
      
      // رموز خاصة
      if (/[!@#$%^&*(),.?":{}|<>]/.test(password)) strength++;
      
      if (strength <= 2) {
        text = 'ضعيفة';
        className = 'strength-weak';
      } else if (strength <= 3) {
        text = 'متوسطة';
        className = 'strength-medium';
      } else {
        text = 'قوية';
        className = 'strength-strong';
      }
      
      return { strength, text, className };
    }

    function validatePasswords() {
      const password = document.getElementById('password');
      const confirmPassword = document.getElementById('confirmPassword');
      
      if (confirmPassword.value && password.value !== confirmPassword.value) {
        confirmPassword.setCustomValidity('كلمات المرور غير متطابقة');
        confirmPassword.classList.add('invalid');
        confirmPassword.classList.remove('valid');
        return false;
      } else {
        confirmPassword.setCustomValidity('');
        if (confirmPassword.value) {
          confirmPassword.classList.remove('invalid');
          confirmPassword.classList.add('valid');
        }
        return true;
      }
    }

    document.addEventListener('DOMContentLoaded', () => {
      console.log('🔐 صفحة إعادة تعيين كلمة المرور جاهزة');
      
      const form = document.getElementById('resetForm');
      const passwordInput = document.getElementById('password');
      const confirmPasswordInput = document.getElementById('confirmPassword');
      const strengthIndicator = document.getElementById('passwordStrength');
      const strengthBar = document.getElementById('strengthBar');
      const strengthText = document.getElementById('strengthText');

      // تحديث مؤشر قوة كلمة المرور
      passwordInput.addEventListener('input', function() {
        const password = this.value;
        
        if (password.length > 0) {
          strengthIndicator.style.display = 'block';
          const result = checkPasswordStrength(password);
          
          strengthBar.className = 'strength-bar ' + result.className;
          strengthText.textContent = result.text;
          strengthText.style.color = result.className === 'strength-weak' ? 'var(--danger)' : 
                                   result.className === 'strength-medium' ? 'var(--warning)' : 'var(--success)';
          
          // التحقق من صحة كلمة المرور
          if (password.length >= 6) {
            this.classList.remove('invalid');
            this.classList.add('valid');
          } else {
            this.classList.remove('valid');
            this.classList.add('invalid');
          }
        } else {
          strengthIndicator.style.display = 'none';
          this.classList.remove('valid', 'invalid');
        }
        
        // إعادة التحقق من تطابق كلمات المرور
        if (confirmPasswordInput.value) {
          validatePasswords();
        }
      });

      // التحقق من تطابق كلمات المرور
      confirmPasswordInput.addEventListener('input', validatePasswords);

      // معالجة إرسال النموذج
      form.addEventListener('submit', (e) => {
        const isValidPasswords = validatePasswords();
        const password = passwordInput.value;
        
        if (!isValidPasswords || password.length < 6) {
          e.preventDefault();
          return;
        }

        const submitBtn = form.querySelector('button[type="submit"]');
        submitBtn.innerHTML = '<span class="loading"></span><span>جاري الحفظ...</span>';
        submitBtn.disabled = true;
      });
    });
  </script>
</body>
</html>